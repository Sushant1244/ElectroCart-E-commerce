(# Environment variables for backend
# PostgreSQL connection for Sequelize (username: postgres, password: root)
# Updated to use DB name without spaces to avoid URL-encoding issues
POSTGRES_URL=postgres://postgres:root@localhost:5432/electrocart_ecommerce

# SMTP settings for Nodemailer (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@electrocart.local

# If MongoDB is used, you can set MONGODB_URI here as well
# MONGODB_URI=mongodb://localhost:27017/electrocart
)
