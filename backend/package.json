{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:nodemon": "nodemon server.js",
    "test": "jest --runInBand --forceExit --detectOpenHandles",
    "seed:pg": "node seed_pg.js",
    "migrate:create-reviews": "node scripts/create_reviews_table.js",
    "migrate:cleanup-notifications": "node scripts/cleanup_old_notifications_migration.js",
    "build": "cd ../frontend && npm install && npm run build",
    "heroku-postbuild": "cd ../frontend && npm install && npm run build"
  },
  "jest": {
    "setupFilesAfterEnv": ["./test/setup.js"],
    "teardownFilesAfterEnv": ["./test/teardown.js"],
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.test.js"],
    "verbose": true
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.400.0",
    "axios": "^1.13.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.4",
    "nodemailer": "^7.0.11",
    "pg": "^8.16.3",
    "sequelize": "^6.37.5",
    "sequelize-cli": "^6.6.2",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^2.0.22",
    "supertest": "^7.2.2"
  }
}
