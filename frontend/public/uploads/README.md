Place your logo image(s) here. Recommended filenames:


Example: copy the provided image into this folder as `logo.png` so the header and favicon will show it.
Example: copy the provided image into this folder as `logo.png` so the header and favicon will show it.
